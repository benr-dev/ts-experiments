{
  "name": "@benr-ts-experiments/lib1",
  "version": "1.1.0",
  "description": "",
  "main": "dist/src/index",
  "scripts": {
    "build": "tsc --build tsconfig-build.json",
    "build:test": "tsc --build tsconfig-test.json",
    "clean": "rimraf dist coverage out",
    "test": "npm run build && npm run build:test && jest"
  },
  "author": "benr-dev",
  "license": "MIT",
  "files": [
    "dist"
  ]
}
