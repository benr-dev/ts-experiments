{
  "name": "ts-experiments",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "nx run-many --target build",
    "build:test": "nx run-many --target build:test",
    "clean": "nx run-many --target clean",
    "test": "nx run-many --target test",
    "pretty": "prettier --write .",
    "lerna-echo-concurrently": "nx run-many --target echo-concurrently",
    "precommit": "nx run-many --target precommit",
    "disabled_prepush": "npm run version",
    "prepare": "husky install",
    "version": "npx lerna version --conventional-commits --no-push"
  },
  "repository": {
    "type": "git",
    "url": "github.com:benr-dev/ts-experiments.git"
  },
  "author": "benr-dev",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "7.x",
    "@babel/preset-env": "7.x",
    "@babel/preset-typescript": "7.x",
    "@tsconfig/node14": "1.x",
    "@types/jest": "28.x",
    "babel-jest": "28.x",
    "husky": "^8.0.3",
    "jest": "28.x",
    "lerna": "^5.6.2",
    "nx": "^14.7.13",
    "prettier": "2.x",
    "rimraf": "3.x",
    "ts-node": "^10.8.1",
    "typescript": "4.x"
  },
  "engines": {
    "node": ">=16"
  },
  "workspaces":[
    "packages/*"
  ]
}
